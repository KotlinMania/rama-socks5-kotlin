# Port report — `rama-socks5-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `rama-socks5`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 6 `use` sites from 1 other repos
- **Outbound**: 189 `use` sites to 5 other repos
- **Unresolved external**: 4 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `rama-socks5` | `rama_socks5` | 6 | 5 | COD-110 | 74 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (6 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-network-proxy` | 6 | ✅ |

### Symbol-level demand (6 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `rama_socks5::Socks5Acceptor` | 1 | 1 | 0 | 1 |
| `rama_socks5::server::DefaultConnector` | 1 | 1 | 0 | 1 |
| `rama_socks5::server::DefaultUdpRelay` | 1 | 1 | 0 | 1 |
| `rama_socks5::server::udp::RelayDirection` | 1 | 1 | 0 | 1 |
| `rama_socks5::server::udp::RelayRequest` | 1 | 1 | 0 | 1 |
| `rama_socks5::server::udp::RelayResponse` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 6 symbols)

##### `rama_socks5::Socks5Acceptor` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/socks5.rs:31` — `use rama_socks5::Socks5Acceptor`

##### `rama_socks5::server::DefaultConnector` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/socks5.rs:32` — `use rama_socks5::server::DefaultConnector`

##### `rama_socks5::server::DefaultUdpRelay` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/socks5.rs:33` — `use rama_socks5::server::DefaultUdpRelay`

##### `rama_socks5::server::udp::RelayDirection` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/socks5.rs:503` — `use rama_socks5::server::udp::RelayDirection`

##### `rama_socks5::server::udp::RelayRequest` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/socks5.rs:34` — `use rama_socks5::server::udp::RelayRequest`

##### `rama_socks5::server::udp::RelayResponse` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-network-proxy` `src/socks5.rs:35` — `use rama_socks5::server::udp::RelayResponse`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `tokio-kotlin` | 22 | 7 | `tokio` | COD-156 | 10 | To Do |
| `rand-kotlin` | 2 | 2 | `rand` | COD-114 | 14 | To Do |
| `rama-core-kotlin` | 93 | 28 | `rama-core` | COD-106 | 48 | To Do |
| `rama-net-kotlin` | 65 | 20 | `rama-net` | COD-109 | 52 | To Do |
| `rama-tcp-kotlin` | 7 | 5 | `rama-tcp` | COD-111 | 59 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `rama-utils` | 196 | 36 | `rama-socks5` |
| `rama-dns` | 12 | 5 | `rama-socks5` |
| `byteorder` | 6 | 4 | `rama-socks5` |
| `rama-udp` | 6 | 2 | `rama-socks5` |
