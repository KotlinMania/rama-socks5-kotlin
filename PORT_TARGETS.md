# Port targets — `rama-socks5-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `rama-socks5` that codex-rs uses, and therefore the minimum subset that `rama-socks5-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-110] (rank `74`, tier `direct`, status `To Do`)

**Cargo package name**: `rama-socks5`
**Rust module name (in `use` statements)**: `rama_socks5`

## Summary

- **6** distinct symbols imported by codex-rs
- **6** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `rama_socks5::Socks5Acceptor`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/socks5.rs:31` (`codex-network-proxy`) — `use rama_socks5::Socks5Acceptor`

### `rama_socks5::server::DefaultConnector`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/socks5.rs:32` (`codex-network-proxy`) — `use rama_socks5::server::DefaultConnector`

### `rama_socks5::server::DefaultUdpRelay`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/socks5.rs:33` (`codex-network-proxy`) — `use rama_socks5::server::DefaultUdpRelay`

### `rama_socks5::server::udp::RelayRequest`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/socks5.rs:34` (`codex-network-proxy`) — `use rama_socks5::server::udp::RelayRequest`

### `rama_socks5::server::udp::RelayResponse`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/socks5.rs:35` (`codex-network-proxy`) — `use rama_socks5::server::udp::RelayResponse`

### `rama_socks5::server::udp::RelayDirection`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-network-proxy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `network-proxy/src/socks5.rs:503` (`codex-network-proxy`) — `use rama_socks5::server::udp::RelayDirection`
