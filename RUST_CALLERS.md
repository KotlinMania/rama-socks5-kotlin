# Rust callers — `rama-socks5-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `rama-socks5` | `rama_socks5` | 6 | 1 | 1 | 6 |

## `rama-socks5`

### Inbound symbols (6 distinct, 6 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `rama-socks5`.

#### `rama_socks5::Socks5Acceptor` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/socks5.rs:31` — `use rama_socks5::Socks5Acceptor`

#### `rama_socks5::server::DefaultConnector` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/socks5.rs:32` — `use rama_socks5::server::DefaultConnector`

#### `rama_socks5::server::DefaultUdpRelay` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/socks5.rs:33` — `use rama_socks5::server::DefaultUdpRelay`

#### `rama_socks5::server::udp::RelayDirection` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/socks5.rs:503` — `use rama_socks5::server::udp::RelayDirection`

#### `rama_socks5::server::udp::RelayRequest` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/socks5.rs:34` — `use rama_socks5::server::udp::RelayRequest`

#### `rama_socks5::server::udp::RelayResponse` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-network-proxy` `src/socks5.rs:35` — `use rama_socks5::server::udp::RelayResponse`
